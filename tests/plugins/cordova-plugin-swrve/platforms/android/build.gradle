repositories {
    mavenCentral()
}

android {
    lintOptions {
        abortOnError false
    }
}

dependencies {
    // Use all minor versions up to (but excluding) the next major version. 
    api 'com.swrve.sdk.android:swrve-firebase:10.+'
}
